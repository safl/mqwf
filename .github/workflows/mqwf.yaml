---
name: mqwf

on:
  pull_request:
    branches: [main, next]
  push:
    branches: [main, next]
    tags: ["v*"]

defaults:
  run:
    shell: bash

jobs:
  fast:
    runs-on: ubuntu-latest

    steps:
    - name: Something fast
      run: |
        hostname

  slow:
    runs-on: ubuntu-latest

    steps:
    - name: Something slow
      run: |
        sleep 10
        hostname
